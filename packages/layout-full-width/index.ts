// This layout has no custom components — `wide` alone is enough, so this
// package is just a re-export of its own metadata. Compare to
// `layout-minimal` or `layout-with-aside`, which add a component reference
// here for the app's dispatchers to render.
export { fullWidthMeta as fullWidthLayout } from './layout.meta';
