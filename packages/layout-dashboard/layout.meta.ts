// Zero-import metadata only — see layout-full-width/layout.meta.ts for why.
export const dashboardMeta = {
	id: 'dashboard',
	/** Same width-reclaiming behavior as full-width; see that package. */
	wide: true,
} as const;
