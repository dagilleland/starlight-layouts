// Zero-import metadata only — see layout-full-width/layout.meta.ts for why.
export const withAsideMeta = {
	id: 'with-aside',
	// Not `wide`: this layout keeps Starlight's default two-column width,
	// it only *adds* a panel to the existing right-hand column.
} as const;
